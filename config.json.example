{
  "_comment": "本文件是可选的，推荐使用 .env 配置 KB_ROOT（可直接粘贴 Windows 单反斜杠路径）。",
  "_comment_path": "如果一定要用 JSON 配置，Windows 路径必须用正斜杠 / 或双反斜杠 \\\\。单反斜杠会触发 JSON 转义错误。",
  "kb_root": "D:/你的知识库路径",

  "_comment_data_dir": "可选。索引数据目录。留空则默认 <kb_root>/.kb_index",
  "data_dir": ""
}
